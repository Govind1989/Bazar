 Page Type        │ URL                                    │ Logic                                              │
  ├──────────────────┼────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ Marketplace Home │ http://localhost:3000/                 │ Shows the main "Better way to trade" landing page. │
  │ Category Hub     │ http://localhost:3000/foods            │ Renders the CategoryView for Foods.                │
  │ Category Hub     │ http://localhost:3000/electronics      │ Renders the CategoryView for Electronics.          │
  │ Vendor Store     │ http://localhost:3000/apple            │ Renders the Minimal Template for Apple NP.         │
  │ Vendor Store     │ http://localhost:3000/himalayan-bakery │ Renders the Bold Template for Himalayan Bakery.    │
  │ Product Detail   │ http://localhost:3000/p/iphone-15-pro  │ Direct Product Page (via the /p/ route).           │
  │ Services Hub     │ http://localhost:3000/services         │ Shows the list of available services.  


Static Credentials for Testing:
   * Admin: admin@bazar.com / admin
   * Vendor: vendor@himalaya.com / vendor
   * User: user@example.com / user
   * MultiUser: multi@example.com / multi
   


  $env:ANALYZE="true"; npm run build                                                                                                                                       ? for shortcuts
──────────────────────────────────────────────────────────
                                    
In Navbar, after login, currently user is able to logout when clicked on the capital initials name icon when user clicks, create two simple drop down menu , Dashboard and logout. 
On click of the Dashboard open the logged in user dashboard and on logout option logout the user from the application.Move the current
dashboard icon from navbar to the drop down and logout from the intial name icons to the logout dropdown option. Impress me.

Remove the Add product we have on top of the vendor dashbaord, place the functional theme toggle and functional language toggle in 
place of the Add product bottom in the vendor dashboard. Also make the sidebar easily expandable and collapsible in our desktop version.
 When collapsed hide the labels only show icons but on hover the labels shall be displayed.


Study @AgenticTruth and study the current implementation added in our repo and then add the detail of the implementation of our
development process and update the missing module or detail in our @AgenticTruth adhering to the current format preserving the 
current informaton in the file. Impress me.